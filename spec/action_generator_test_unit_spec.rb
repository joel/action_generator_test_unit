# frozen_string_literal: true

RSpec.describe ActionGeneratorTestUnit do
  it "has a version number" do
    expect(ActionGeneratorTestUnit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
